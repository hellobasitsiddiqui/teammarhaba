
### 2026-06-23 agent-A — Dependabot major policy + a false-green trap (TM-136)
- **Investigation result:** the 4 open Actions/Docker major PRs (#114 fetch-metadata 2→3, #116 codeql-action 3→4, #117 auth 2→3, #118 build-push 6→7) are ALL GREEN. "Dep bumps break CI" actually traces to *maven* majors (Spring Boot/springdoc, #56, already ignored), not Actions.
- **False-green trap:** #116 (codeql-action 3→4) is green only because the PR gate **path-skips CodeQL on workflow-only changes** — the bumped action was never exercised. Don't trust a green dep PR until you confirm the bumped thing ran (not `skipping`).
- **Policy decision (option b):** mirror maven's `ignore: version-update:semver-major` into github-actions + docker → majors are deliberate/hand-done in every ecosystem. Minor/patch still flow (SHA-pin freshness, TM-59). `dependabot-auto-label.yml` stays as a backstop for security-driven majors. **Triaged:** closed #114/#116/#117/#118 (were green; redo by hand if wanted), #115 already closed. PR for the .yml is human-merged (not docs auto-merge).

### 2026-06-23 agent-A — `deploy`-label auto-trigger (TM-156)
- **Label a PR `deploy` → its merge auto-deploys `main`** (web + backend). Implemented IN `deploy.yml` (added `pull_request: types: [closed]`), NOT a separate dispatcher — a `GITHUB_TOKEN`-dispatched `gh workflow run` can't start another workflow (the TM-148 trap). Both jobs gated: `if: workflow_dispatch OR (pull_request.merged && contains(labels,'deploy'))`. Unlabelled merge → both jobs skip → deploys nothing. Manual *Deploy → Run* button still works.
- **Critical SHA gotcha:** on a `pull_request` event `github.sha` is the ephemeral PR **test-merge** commit — CI never built/pushed an image for it, and it's not what's on `main`. Resolve `DEPLOY_SHA = github.event.pull_request.merge_commit_sha || github.sha` (workflow-level env) and use it everywhere: the backend image wait/deploy (`${IMAGE}:${DEPLOY_SHA}`) AND the web `actions/checkout` `ref:` (so `git describe` + the built bundle are the real merge commit). On a manual run merge_commit_sha is empty → falls back to main's head.
- **Concurrency:** changed group from `deploy-…-${{ github.ref }}` to a constant `deploy-${{ github.workflow }}`. The two triggers arrive on different refs (`refs/heads/main` vs `refs/pull/N/merge`); keying on ref would let a manual deploy and a label-merge deploy run in parallel and clobber. Constant group + `cancel-in-progress: false` serializes all deploys to the one live env.
- **`deploy` label created** in the repo (green, one-time `gh label create`). Verify-after-merge: this PR is UNLABELLED, so merging it must deploy nothing (clean negative test); the positive path is exercised by the first PR you actually label `deploy`.

### 2026-06-23 agent-A — Web asset fingerprinting + cache headers (TM-144)
- **Deploy now content-hashes web assets** (cache-bust): `web/tools/fingerprint.mjs` (framework-free, Node built-ins) renames `web/dist/assets/*.{js,css}` → `name.<10hex>.ext`, rewriting `index.html` refs AND the ES-module import specifiers. **Transitive hashing** — a file's hash is computed after its deps' specifiers are rewritten, so changing one module busts every importer (a cached importer can't point at a 404'd dep). Runs in `deploy.yml` + `preview.yml` AFTER the `config.js` injections.
- **`firebase.json` headers:** `**` → `Cache-Control: no-cache` (entry always revalidated), `/assets/**` → `public, max-age=31536000, immutable` (listed last so it wins for assets). Net: returning users get new CSS/JS on a normal reload — no hard refresh.
- **`index.html` stays unhashed** (it's the entry). Comment examples like `// import { x } from "./api.js"` are intentionally NOT rewritten (only real import statements are) — harmless, they don't load.
- **Test:** `web/tools/fingerprint.test.mjs` (node's built-in runner) covers rename/hash, ref + specifier rewriting, determinism, and transitive bust. New **`web-build` job in `ci.yml`** runs it on the PR gate (`node --test web/tools/*.test.mjs` — unquoted so the shell globs; portable across Node versions). Fast (Node-only), unlike the main-only e2e.
- **e2e unaffected** — it serves `web/src` (unhashed) directly; only `web/dist` is fingerprinted at deploy.
- **Follow-up:** the nginx container path (web/Dockerfile + nginx.conf, used by the WebView bundle) still serves stable URLs — fingerprint it too if that path needs the same guarantee.
