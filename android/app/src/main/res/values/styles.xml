<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <!-- Launch theme (TM-299). Uses the Android 12+ SplashScreen API: the window background is the
         TeamMarhaba teal (matching capacitor.config.json SplashScreen.backgroundColor and the
         @drawable/splash bitmap) so the very first frame is branded teal, not a white flash, while the
         hosted web UI loads. The legacy splash bitmap is kept as android:background for pre-12 devices.
         @capacitor/splash-screen then holds/hides the splash on the real web-ready signal (launchAutoHide
         is false). -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <item name="windowSplashScreenBackground">@color/splashBackground</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>