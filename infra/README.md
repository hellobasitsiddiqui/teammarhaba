# infra

Infrastructure & deployment config — GCP project setup, Cloud Run, Cloud SQL, Firebase Hosting, CI/CD workflows.

## Contents

- [`gcp/`](./gcp/) — the GCP/Firebase project (`teammarhaba`): config of record
  ([`gcp/config.yaml`](./gcp/config.yaml)) + how it was provisioned
  ([`gcp/README.md`](./gcp/README.md)). (TM-66)

More lands in later tickets (Cloud SQL, Cloud Run/Hosting deploys, OIDC).
